extends layout

block content
  h1= title
  p Welcome to #{title}
  div
    div #{test}
    label(for='ssoToken') SSO Token:
    input#ssoToken(type='text')
  div
    button#getToken(type='button') Get SSO Token
    button#forceConsent(type='button') Force Consent
